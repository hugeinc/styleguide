<div class="columns small-4 huge_toggle-menu-out huge_toggle-menu"><a href="#">Menu</a></div>
<aside class="huge_aside"> 
  <div class="columns small-4 huge_toggle-menu-in huge_toggle-menu"><a href="#">Menu</a></div>
  <div class="clearfix huge_client">
    <h3 class="columns small-12"><img src="styleguide/assets/images/{{logoImage}}" id="logo"/></h3>
  </div>
  <div class="clearfix row">
    <nav>
      <ul>
        {{#each modules}}
          <li>
              <a href="#{{id}}" id="{{id}}" target="sg-viewport">{{name}}</a>
          </li>
        {{/each}}
      </ul>
    </nav>
  </div>
</aside>