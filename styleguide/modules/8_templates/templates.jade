h2.title=name
if items.intro
	each paragraph in items.intro
		p!=paragraph
ul.templates-group
	each template in items.templates
		li
			if template.link
				a(href=template.link)
					img(src='assets/images/#{template.thumbnailFile}', width='200', alt='#{template.templateName}')
					.template-name=template.templateName
			else
				img(src='assets/images/#{template.thumbnailFile}', width='200', alt='#{template.templateName}')
				.template-name=template.templateName